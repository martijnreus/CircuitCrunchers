# create a grid, with a width, heigth and a amount of layers
class grid:

    def __init__(self, width, heigth, layers):
        self.width = width
        self.heigth = heigth
        self.layers = layers